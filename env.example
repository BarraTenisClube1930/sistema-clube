# Copie este arquivo para .env e preencha os valores
# NUNCA suba o arquivo .env para o GitHub

# URL do banco PostgreSQL (Railway ou Supabase)
# Deixe em branco para usar SQLite local
DATABASE_URL=

# Porta do servidor (deixe em branco — usa 3000 por padrão localmente)
PORT=
